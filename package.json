{
  "name": "salon-poke",
  "version": "1.0.0",
  "description": "Salon Poke — Bristol's home for sealed Japanese Pokemon TCG. Static site for Vercel.",
  "private": true,
  "scripts": {
    "build": "echo Static site — no build step needed",
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "deploy-preview": "vercel"
  }
}
